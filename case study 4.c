#include<stdio.h>
#include<conio.h>
void main()
{
	/* ideology : store the elements of rock in one array. put the ascii value of each
	element in seperate arrays and compare the values. */
	int num;
	char elem;
	printf("Enter number of rocks : ");
	scanf("%d",&num);
	while (i<=num)
	{
		printf("Enter the elements in rock %d",num);
		scanf("%s",&elem);
		arr[i]=elem;
	}
	
}
